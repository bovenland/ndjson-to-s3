{
  "name": "@bovenland/ndjson-to-s3",
  "version": "1.0.0",
  "description": "Creates S3 objects from lines in newline delimited JSON file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bert Spaan",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1053.0",
    "highland": "^2.13.5"
  }
}
